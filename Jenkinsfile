pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // git 'https://github.com/sicreji/nuvolapp.git'
                sh '''
                docker --help
                '''
            }
        }
       
    }
}
